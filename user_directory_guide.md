# دليل محتويات المجلد الرئيسي `/home/user/` بالتفصيل

هذا الدليل يشرح بالتفصيل الممل كل ملف ومجلد متواجد في مجلد المستخدم الرئيسي للوكيل الذكي (`/home/user/`) لكي تفهم بيئة العمل بشكل كامل.

---

## 1. الملفات النصية المباشرة (Direct Text Files)

### أ. ملف إعدادات الوكيل: `AGENT.md`
* **نوع الملف:** ملف إعدادات بصيغة Markdown + YAML Frontmatter.
* **الوظيفة:** هذا هو الملف الرئيسي الذي يحدد هوية الوكيل (Agent Profile). يحتوي على الاسم المعروض، الوصف الشخصي، الأيقونة، والتعليمات البرمجية التي تحكم تصرفات الوكيل.
* **المحتوى الحرفي والكامل للملف:**
```yaml
---
name: My First Agent
description: An AI agent built during onboarding
icon: icon-1
---

You are a helpful AI assistant. Use the tools available to you to answer questions and complete tasks for the user.
```

### ب. ملف مجمع المهارات: `all_skills_details.md`
* **نوع الملف:** ملف Markdown (تم إنشاؤه في هذه الجلسة).
* **الوظيفة:** يحتوي على تجميع كامل ومفصل لكل الكود والتعليمات المتواجدة داخل مجلد المهارات الـ 8 (`skills`).

---

## 2. المجلدات الرئيسية (Main Directories)

### أ. مجلد المهارات: `skills/`
* **الوظيفة:** يحتوي على المجلدات والملفات والسكربتات الخاصة بالمهارات الثمانية المتوفرة للوكيل (مثل مهارة التعامل مع ملفات Excel، استدعاء أدوات التكامل، وبناء واجهات الويب).

### ب. مجلد المرفوعات: `.uploads/` (مجلد مخفي)
* **الوظيفة:** هذا هو المجلد الذي يستقبل ويحفظ فيه النظام أي ملف تقوم أنت (المستخدم) برفته في المحادثة لكي أتمكن من قراءته ومعالجته برمجياً.

### ج. مجلد المخرجات الضخمة: `.spillover/` (مجلد مخفي)
* **الوظيفة:** عندما تقوم أداة برمجية بإرجاع نص ضخم جداً يفوق استيعاب واجهة الشات، يقوم النظام تلقائياً بحفظ النص الكامل داخل هذا المجلد لكي أتمكن من معالجته باستخدام بايثون دون أن يضيع.

### د. مجلد الكاش: `.cache/` (مجلد مخفي)
* **الوظيفة:** لحفظ ملفات النظام المؤقتة وتسريع بعض العمليات.

---

## 3. ملفات النظام والبيئة المخفية (Hidden System & Env Files)

### أ. ملف حالة بايثون: `.python_state.pkl`
* **نوع الملف:** ملف ثنائي (Binary Pickle File).
* **الوظيفة:** هذا الملف يقوم بحفظ كاع المتغيرات والداتا (Variables, DataFrames, dicts) اللي خدمنا بيهم فـ Python. بفضل هذا الملف، يلا عرفت متغير في أمر سابق، كنقدر نستخدمو فالأمر اللي تابعو بلا ما يضيع.

### ب. ملف بيئة العمل: `.gumcp_env`
* **الوظيفة:** يحتوي على إعدادات بيئة العمل والربط البرمجي الخاص بالـ MCP لتشغيل الأدوات الخارجية.

### ج. ملفات النظام القياسية للينكس (Linux Dotfiles):
* **`.bashrc`**: سكربت كيتشغل تلقائياً فاش كتفتح Shell جديدة لتجهيز المتغيرات والإعدادات.
* **`.profile`**: ملف إعدادات الجلسة للمستخدم عند الدخول.
* **`.bash_logout`**: تعليمات كيتشغلو فاش كتسد الجلسة.
